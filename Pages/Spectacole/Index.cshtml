@page
@model Spectacole_Teatru_Project.Pages.Spectacole.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Spectacol[0].Nume_Piesa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Spectacol[0].Autor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Spectacol[0].Pret)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Spectacol[0].SpectacolDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Spectacol[0].Regizor.Nume_Regizor)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Spectacol) {
        
     
         
            <td>
                 @Html.DisplayFor(modelItem => item.Nume_Piesa)
            </td>
            <td>
                 @Html.DisplayFor(modelItem => item.Autor)
            </td>
             <td>
           
                 @Html.DisplayFor(modelItem => item.Pret)
            </td>
            <td>
                 @Html.DisplayFor(modelItem => item.SpectacolDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Regizor.Nume_Regizor)
            </td>
            
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> 
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> 
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
           
        
}
    </tbody>
</table>
}